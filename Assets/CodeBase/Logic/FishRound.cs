using System;
using UnityEngine;

namespace CodeBase.Logic
{
  public class FishRound : MonoBehaviour
  {
    private void Update()
    {
   //   Quaternion rorationY = Quaternion.AngleAxis(0.5f, Vector3.forward);
   //   transform.rotation *= rorationY;
    }
  }
}